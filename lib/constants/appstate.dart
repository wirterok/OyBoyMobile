enum AuthStage { uninitialized, unauthorized, initialized, authorized, error }

enum PageType { 
  splash, 
  login, 
  register,
  video, 
  short, 
  stream, 
  profile, 
  search, 
  create, 
  settings,
  detail
}
